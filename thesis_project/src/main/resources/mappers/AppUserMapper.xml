<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="thesis.project.gu.user.persistence.AppUserMapper">

    <resultMap id="userMap" type="thesis.project.gu.user.domain.AppUser">
        <id     column="id"           property="id"/>
        <result column="display_name" property="displayName"/>
        <result column="created_at"   property="createdAt"/>
        <result column="updated_at"   property="updatedAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_user (display_name) VALUES (#{displayName})
    </insert>

    <select id="findById" resultMap="userMap">
        SELECT * FROM app_user WHERE id = #{id}
    </select>

    <select id="findByDisplayName" resultMap="userMap">
        SELECT * FROM app_user WHERE display_name = #{name} LIMIT 1
    </select>

    <select id="existsByDisplayName" resultType="boolean">
        SELECT COUNT(1) > 0 FROM app_user WHERE display_name = #{name}
    </select>
</mapper>
